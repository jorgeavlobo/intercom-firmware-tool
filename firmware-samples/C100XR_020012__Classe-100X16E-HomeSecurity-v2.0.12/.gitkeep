# Placeholder so this (otherwise binary-only) firmware folder is tracked in git.
#
# Device : BTicino Classe 100X16E
# App    : Home + Security
# Version: v2.0.12 (02.00.12)
#
# Source : C100XR_020012.fwz — the tool-recognised artifact, registered in
#          IntercomFirmwareTool.Core/FirmwareRegistry.cs (exact size +
#          SHA-256/MD5).
#          https://www.homesystems-legrandgroup.com/MatrixENG/liferay/bt_mxLiferayCheckout.jsp?fileFormat=generic&fileName=C100XR_020012.fwz&fileId=58107.23188.36439.38772
#          Extract btweb_only.ext4.gz from it and drop it here.
#
# Note   : Determine the app from the BTicino download page / image metadata,
#          NOT from the .fwz stem — the stem is not a reliable discriminator
#          (e.g. C100X_010501.fwz is also Home + Security). These v2.x releases
#          just happen to use the "C100XR" stem; same 100X16E hardware,
#          different firmware per app.
#
# See ../README.md ("App axis" + "Adding a new sample").
