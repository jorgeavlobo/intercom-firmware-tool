# Placeholder so this (otherwise binary-only) firmware folder is tracked in git.
#
# Device : BTicino Classe 100X16E
# App    : Home + Security
# Version: v2.3.11 (02.03.11)
#
# Source : C100XR_020311.fwz — the tool-recognised artifact, registered in
#          IntercomFirmwareTool.Core/FirmwareRegistry.cs (exact size +
#          SHA-256/MD5). https://assets.legrand.com/pim/AUTRE/C100XR_020311.fwz
#          Extract btweb_only.ext4.gz from it and drop it here.
#
# Note   : Home + Security firmware uses the "C100XR" stem (vs "C100X" for the
#          Door Entry app) — same 100X16E hardware, different firmware per app.
#
# See ../README.md ("App axis" + "Adding a new sample").
